apiVersion: v2
name: hello-world
description: Minimal HTTP origin that serves a fixed string — the in-cluster target for the cloudflared tunnel.
type: application
version: 0.1.0
appVersion: "1.27-alpine"
