# GHCR pull credentials. `password` (a read:packages PAT) is supplied
# SOPS-encrypted via environments/<env>/secrets/container-registry-credentials.yaml
# and overrides the empty default below. server/username are not secret.
server: ghcr.io
username: joegoggins
password: ""
