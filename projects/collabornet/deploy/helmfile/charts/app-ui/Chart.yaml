apiVersion: v2
name: app-ui
description: Collabornet app UI — nginx hello-world placeholder (React app, incl. /radius and /api proxy, lands later).
type: application
version: 0.1.0
appVersion: "0.1.0"
